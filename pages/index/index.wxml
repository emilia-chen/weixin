<!--wxml-->
<wxs module="m1">
module.exports={
 msg:'hello world'
}
</wxs>
<wxs src="./index.wxs" module="m2"></wxs>



<!--index.wxml-->
<view class="container">
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称1 </button>
    <block wx:else>
      <image  class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>{{m2.msg}}
    </block>
  </view>
  <view bindtap="bindViewTap" data-shopId='123'>去日志页面</view>
  <view bindtap="bindAnimationTap">去动画页面</view>
  <view bindtap="bindSwiperTap">去滑动页面</view>
  <view bindtap="useHttp">调用接口</view>
  <view bindtap="scanCode">扫码</view>
  <view bindtap="changeVlaue">修改值</view>
  <view>{{motto}}</view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
</view>


